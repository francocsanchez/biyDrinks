import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor:"#fff",
  },
  listCard:{
    marginTop:10,
    marginBottom:60,
  }
});

export { styles };
