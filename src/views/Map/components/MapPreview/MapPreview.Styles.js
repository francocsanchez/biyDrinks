import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mapView: {
    justifyContent: "center",
    alignItems: "center",
  },
  mapElement: {
    width: "100%",
    height: 700,
  },
});

export { styles };
